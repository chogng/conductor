name: Desktop CI

on:
  pull_request:
    paths:
      - "desktop/**"
      - "src/**"
      - "build/**"
      - "scripts/build-desktop.mjs"
      - "build/win32/verify-icons.ts"
      - "build/win32/verify-auto-update-config.ts"
      - "package.json"
      - "package-lock.json"
      - "build/github/upload-release-assets.ps1"
      - "src/tsconfig.desktop.json"
      - "vite.config.js"
      - ".github/workflows/desktop-ci.yml"
      - ".github/workflows/release.yml"
      - ".github/workflows/release-darwin.yml"
      - ".github/workflows/release-web.yml"
      - ".github/workflows/release-windows.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  desktop-validate:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup shared Windows toolchain
        uses: ./.github/actions/setup-windows-node-python

      - name: Install dependencies
        run: npm ci

      - name: Verify auto-update config
        run: npm run verify:auto-update-config

      - name: Build desktop artifacts
        run: npm run build:desktop

      - name: Validate desktop packaging
        run: npx electron-builder --publish never
