// Sidebar-level shell for the import contribution.
// This will host the workbench sidebar wiring once the migration lands.
export type ImporterViewletProps = {
  readonly id?: string;
};

const ImporterViewlet = (_props: ImporterViewletProps) => null;

export default ImporterViewlet;
