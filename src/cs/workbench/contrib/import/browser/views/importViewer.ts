// Presentational layer for the importer surface.
// Rendering for empty state, item list, and file cards will live here.
export type ImportViewerProps = {
  readonly id?: string;
};

const ImportViewer = (_props: ImportViewerProps) => null;

export default ImportViewer;
