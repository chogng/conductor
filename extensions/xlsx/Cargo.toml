[package]
name = "xlsx"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib", "cdylib"]

[dependencies]
calamine = { version = "0.36", default-features = false, features = ["dates", "picture"] }
quick-xml = "0.38"
serde_json = "1.0"
